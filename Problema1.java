public class Problema1 {
    //Problema 1
    //Respecto los programas Proc1 y Proc2, que se dan a continuación, responded a las cuestiones
    //siguientes:

    public static void main(String[] args) {

        //a) ¿Cuál es el propósito de la función dbl de Proc1 (qué calcula)?
        //Multiplica por 2 el valor que ponemos como parámetro


        //b) ¿Cuál es el propósito de la función sumpl de Proc2?
        //Suma los dos parámetros que indicamos y al resultado le agrega un 1

        //c) En el programa Proc1 se llama a la función dbl utilizando los parámetros a y b en dos ocasiones.
        //Pero si se observa la definición de la función dbl vemos que el parámetro se llama x. ¿Tiene alguna
        //importancia esta discrepancia?

        //Ninguna. Podemos llamar al parámetro como queramos y no afectara al resultado final.

        //d) En el programa Proc2 se hace una invocación de la función sumpl con parámetros b y a (en este
        //orden), pero si observamos la definición de la función vemos que los parámetros son a y b (en este
        //orden). ¿Esta discrepancia (a y b en un lugar y b y a en el otro) tiene alguna importancia?

        //Ninguna, porque a la hora de sumar no importa el orden en que estén los parámetros.

        //e) El programa Proc1, ¿qué resultado escribe si se le da el valor 4? ¿Y si se le da el valor -8? ¿Que
        //hace Proc1?

        // Si le damos valor 4 nos devolverá 16 y si le damos valor -8 nos devolverá -32

        //f) El programa Proc2, ¿qué resultado produce si se le dan los valores 9 y 7?

        // Nos devolverá 17 y 7


    }
}
